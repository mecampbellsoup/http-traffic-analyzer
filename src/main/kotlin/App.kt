package httptrafficanalyzer

fun main(args: Array<String>) {
    println("Hello, World!")
    // Setup the database
    // Locate the logfile
    // Parse the logfile, creating a `Request` record for each entry
    // Analyze the data which is now in the DB

}
